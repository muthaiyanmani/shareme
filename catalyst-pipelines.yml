version: 2.1
runners:
  slate:
    config-id: 3
jobs: 
  build:
    cwd: /catalyst
    steps:
        - echo "Initializing the deployment process..."
        - pwd
        - npm install --omit=dev --force
        - npm install zcatalyst-cli
stages:
  - name: shareme
    jobs:
      - build